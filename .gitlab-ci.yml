image: node:20

stages:
  - build

build_image:
  stage: build
  tags:
    - backend
  script:
    - docker info  # Vérifier que Docker est bien configuré
    - docker build -t my-image:latest .  # Construire l'image Docker
    - docker run my-image:latest  # Exécuter l'image construite
